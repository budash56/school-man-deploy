# PostgreSQL container settings
POSTGRES_DB=schoolmg
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me

# Backend runtime settings
DATABASE_URL=postgres://postgres:change-me@db:5432/schoolmg
JWT_SECRET=replace-with-a-long-random-secret
SCANNER_BASE_URL=http://scanner:8010
SCANNER_TIMEOUT_MS=15000

# Email settings
# Set EMAIL_ENABLED=false to disable outbound mail and keep the backend in preview mode.
EMAIL_ENABLED=false
EMAIL_PROVIDER=smtp
EMAIL_HOST=smtp.example.com
EMAIL_PORT=465
EMAIL_SECURE=true
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM_NAME=SchoolMan
EMAIL_FROM_ADDRESS=no-reply@example.com
EMAIL_BULK_BATCH_SIZE=20
